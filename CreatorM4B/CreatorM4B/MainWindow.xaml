<Window x:Class="CreatorM4B.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CreatorM4B"
        mc:Ignorable="d"
        Title="Создание файла M4B из файлов MP3"
        Width="700" Height="450" ResizeMode="CanResizeWithGrip"
        MinWidth="500" MinHeight="300"
        d:Width="700" d:Height="350"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="ButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="100" />
                <Setter Property="Height" Value="25" />
            </Style>
            <Style x:Key="ReadOnlyTextBoxStyle" TargetType="TextBox">
                <Setter Property="IsReadOnly" Value="True" />
                <Setter Property="IsReadOnlyCaretVisible" Value="True" />
                <Setter Property="Background" Value="AliceBlue" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button x:Name="FolderButton" Content="Папка"
                        Style="{StaticResource ButtonStyle}"
                        Click="FolderButton_Click" />
                <Button x:Name="CreateButton" Content="Создать"
                        IsEnabled="False"
                        Margin="10,0,0,0"
                        Style="{StaticResource ButtonStyle}"
                        Click="CreateButton_Click" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="AboutButton" Content="О программе"
                        Style="{StaticResource ButtonStyle}" Click="AboutButton_Click" />
                <Button x:Name="CloseButton" Content="Закрыть"
                        Margin="10,0,0,0" Style="{StaticResource ButtonStyle}" Click="CloseButton_Click" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,16,0,0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Папка" Margin="0,0,8,0" />
            <TextBox Grid.Column="1" x:Name="FolderTextBox"
                     Style="{StaticResource ReadOnlyTextBoxStyle}" />
        </Grid>
        
        <TextBlock Grid.Row="2" Text="Файлы MP3" Margin="0,10,0,0" />

        <TextBox Grid.Row="3" x:Name="FilesTextBox"
                 HorizontalScrollBarVisibility="Auto"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,4,0,10"
                 Style="{StaticResource ReadOnlyTextBoxStyle}" />

        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Text="Папка не выбрана" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
